# Transformer-SentimentScope
A custom Decoder-only Transformer built from scratch in PyTorch for binary sentiment classification on the IMDB dataset. Features global mean pooling and subword tokenization.
